export default function Home() {
  return (
    <main style={{ padding: "3rem", fontFamily: "system-ui", maxWidth: "42rem" }}>
      <h1>Support ticket deflection & reply drafting</h1>
      <p>
        Scaffold only — no feature is built yet. The first milestone that needs
        a running app is where this stops being a placeholder. See
        <code> docs/prd/ </code> for what each phase must deliver.
      </p>
    </main>
  );
}
